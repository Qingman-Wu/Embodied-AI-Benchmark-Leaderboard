const state={rows:[],filtered:[]};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const label={"Open-source VLA":['VLA','vla'],"Foundation Robot Model":['Foundation','foundation'],"World Action Model":['WAM','wam'],"Reference Policy":['Reference','reference']};
function render(){const q=document.querySelector('#search').value.toLowerCase(),track=document.querySelector('#filter-track').value,suite=document.querySelector('#filter-suite').value,role=document.querySelector('#filter-role').value;state.filtered=state.rows.filter(r=>(!q||JSON.stringify(r).toLowerCase().includes(q))&&(!track||r.category===track)&&(!suite||r.suite===suite)&&(!role||r.result_role===role));document.querySelector('#record-count').textContent=state.filtered.length;document.querySelector('#model-count').textContent=new Set(state.rows.map(r=>r.model)).size;document.querySelector('#leaderboard tbody').innerHTML=state.filtered.map((r,i)=>{const [t,c]=label[r.category]||[r.category,'reference'];const src=r.source_url?`<a href="${esc(r.source_url)}" target="_blank">${esc(r.paper_id)}</a> · ${esc(r.source_table)}`:'—';return `<tr><td><span class="track ${c}">${t}</span></td><td class="model">${esc(r.model)}</td><td class="source">${src}<br><span class="setting">${esc(r.setting)}</span></td>${['spatial','object','goal','long'].map(k=>`<td class="score">${r[k]??'—'}</td>`).join('')}<td class="score avg">${r.average??'—'}</td><td class="role">${r.result_role==='main'?'★ Main':r.result_role==='baseline'?'Baseline':r.result_role}</td><td><button class="details-btn" data-index="${i}">View ↗</button></td></tr>`}).join('')||'<tr><td colspan="10" class="loading">No matching records</td></tr>';document.querySelectorAll('.details-btn').forEach(b=>b.onclick=()=>details(state.filtered[+b.dataset.index]));}
function details(r){document.querySelector('#details-content').innerHTML=`<h2>${esc(r.model)}</h2><p><b>${esc(r.paper)}</b> · ${esc(r.source_table||'')}</p><p>${esc(r.notes||'暂无备注')}</p><p class="setting">Record ID: ${esc(r.id)}</p>`;document.querySelector('#details').showModal();}
async function init(){const res=await fetch('../data/libero_results.jsonl');state.rows=(await res.text()).trim().split('\n').map(JSON.parse);render();}
['search','filter-track','filter-suite','filter-role'].forEach(id=>document.getElementById(id).oninput=render);document.getElementById('reset').onclick=()=>{['search','filter-track','filter-suite','filter-role'].forEach(id=>document.getElementById(id).value='');render()};document.querySelector('.close').onclick=()=>document.querySelector('#details').close();init().catch(e=>document.querySelector('#leaderboard tbody').innerHTML=`<tr><td colspan="10" class="loading">Unable to load data: ${esc(e.message)}</td></tr>`);
